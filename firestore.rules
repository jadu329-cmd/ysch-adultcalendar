rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 읽기/쓰기 허용 (상시 운영용)
    // 주의: 이 규칙은 인증 없이 모든 사용자가 데이터에 접근할 수 있습니다.
    // 프로덕션 환경에서는 보안을 위해 인증 기반 규칙을 사용하는 것을 권장합니다.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
